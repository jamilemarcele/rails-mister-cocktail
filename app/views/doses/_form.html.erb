<%= simple_form_for([ @cocktail, @dose ], remote: true, html: { class: "dose-form"}) do |f| %>
  <%= f.input :description, label: "Amount", placeholder: "10 ml, 6 leaves..." %>
  <%= f.association :ingredient, input_html: { class: 'select2' } %>
  <%= f.submit class: 'btn btn-dark' %>
<% end %>
